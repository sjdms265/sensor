app:
    kafka:
        producer:
            topic: ${sensormanager.topic.temperature}:1:1
server:
    port: 8081
spring:
    application:
        name: sensormanager
    datasource:
        password: postgres
        url: jdbc:postgresql://localhost:5432/sensormanager
        username: postgres
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true
        show-sql: false
    kafka:
        bootstrap-servers: localhost:9092
        producer:
            retries: 0
            key-serializer: org.apache.kafka.common.serialization.StringSerializer
            value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

#    mvc:
#        converters:
#            preferred-json-mapper: jsonb
sensor-manager:
    token:
        secret: secret
        access-expires: 600000
        refresh-expires: 1800000
    topic:
        sensor-value: sensor-value

camel:
    component:
        kafka:
            brokers: localhost:9092
        paho-mqtt5:
            broker-url: 'tcp://192.168.50.4:1883'
            user-name: mosquito
            password: mosquito123
